<% include adminheader %>
<div class="container  my-4">
  <section>
    <h1>Add Index Here</h1>
    <form method="post" action="">
      <h3><%= msg %></h3>
      <div class="form-group">
        <label>Course Name</label>
        <select name="courseList" style="width:70%">
          <option>Select CourseName</option>
          <% for(let row of courseList) { %>
          <option> <%= row.coursenm %></option>
          <% } %>
        </select>
        </td>
        </tr>
      </div>
      <div class="form-group">
        <label>Sub Course Name </label>
        <textarea name="subCourse"></textarea>
        <script>
          CKEDITOR.replace('subCourse');
        </script>
      </div>
      <div class="form-group">
        <label>Course Description</label>
        <textarea name="Description"></textarea>
        <script>
          CKEDITOR.replace('Description');
        </script>
      </div>

      <div class="form-group">
        <button type="submit" class="btn btn-primary">Add Details</button>
      </div>

    </form>
  </section>
</div>

<% include footer %>